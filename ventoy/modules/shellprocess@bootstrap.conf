# ==============================================================================
# Calamares - shellprocess@bootstrap.conf
# Instala pacotes essenciais de bootstrap para o Ansible no sistema instalado
# ==============================================================================
---
dontChroot: false
timeout: 300
script:
  - "apt-get update"
  - "apt-get install -y pipx git curl sudo"
